# Техническое задание на разработку страницы‑калькулятора подбора аккумуляторов

**Проект:** Страница‑калькулятора подбора аккумуляторов  
**Сайт:** <https://zeuselectro.com/>  
**Платформа:** 1С‑Битрикс (редакция «Бизнес»)  
**Заказчик:** ZeusElectro  
**Исполнитель:** Андрей Шевченко (VERDEXBIT)

---

## 1. Цель проекта

Создать интерактивную страницу‑калькулятора, позволяющую покупателю подобрать аккумулятор по техническим параметрам и оформить заказ прямо на сайте.  
Калькулятор должен заменить Excel‑файл, которым сейчас пользуются менеджеры при ручных расчётах.

---

## 2. Источник данных

Исходные данные берутся из Excel‑файла `Калькулятор_rucelf.xlsx`. Каждая строка файла — это отдельная модель аккумулятора, содержащая, в том числе:

- Модель аккумулятора (например, `Sacred Sun SP12-100`);
- Номинальное напряжение, ёмкость, тип, массу, производителя;
- Значения ёмкости (или мощности) при разном времени разряда — например, 10, 15, 20, 30, 60 минут и т.д.;
- Служебные расчётные значения (используются для вычислений в калькуляторе).

Эти данные должны быть импортированы в **инфоблок каталога товаров 1С‑Битрикс**.  
Каждая модель аккумулятора — отдельный элемент инфоблока с набором свойств, соответствующих колонкам Excel.

---

## 3. Структура страницы

Страница размещается по адресу, например: `/calculator/`.

### 3.1. Верхняя часть — форма ввода параметров

Форма размещается в верхней части страницы, на десктопе — в 2 колонки.

Поля для ввода пользователем:

1. **Мощность нагрузки, Вт**  
   - Тип: `number`  
   - Обязательное: да  

2. **Кол‑во приборов в линейке**  
   - Тип: `number`  
   - Обязательное: да  

3. **Кол‑во линеек**  
   - Тип: `number`  
   - Обязательное: да  

4. **КПД системы, %**  
   - Тип: `number`  
   - Обязательное: да  

5. **Требуемое время работы, мин**  
   - Тип: `select`  
   - Обязательное: да  
   - Значения по умолчанию: `10, 15, 20, 30, 45, 60, 90, 120` (уточняется по Excel‑файлу)

Кнопка под формой:

- Текст: **«Подобрать»**
- Тип: `<button type="button">`
- Стиль: основная кнопка (Bootstrap `btn btn-primary` или аналогичный).

### 3.2. Алгоритм расчёта

1. Пользователь заполняет форму и нажимает кнопку **«Подобрать»**.
2. На основе введённых параметров выполняется расчёт **мощности на один элемент** — по формуле, аналогичной формуле в Excel, ячейка `AB8` (точная формула берётся из файла):

   - В расчёт входят поля:
     - Мощность нагрузки
     - Кол‑во приборов в линейке
     - Кол‑во линеек
     - КПД системы

3. Рассчитанное значение мощности на один элемент сравнивается со значениями в столбце, соответствующем выбранному пользователем времени (например, «30 мин»), по аналогии с условным форматированием в Excel:
   - Отбираются все модели аккумуляторов, для которых значение в этом столбце **больше либо равно** расчётному значению.

4. Полученный список подходящих аккумуляторов сортируется:
   - по возрастанию «ближайшего превышения» (настроить: по ёмкости, по мощности или по рассчитанному значению);
   - порядок сортировки уточняется с заказчиком (по умолчанию — по возрастанию соответствующей ёмкости/мощности).

5. Результат отображается в таблице под формой.

### 3.3. Блок результатов

Под формой размещается блок:

**Заголовок:** «Результаты подбора»

Таблица с колонками (минимально):

- Модель (кликабельная, ссылка на детальную страницу товара);
- Напряжение;
- Ёмкость;
- Время работы при выбранном режиме;
- Масса;
- Цена (если есть в каталоге);
- Кнопка **«Заказать»**.

Пример строки:

| Модель             | Напряжение | Ёмкость | Время работы | Масса | Цена     |   |
|--------------------|-----------:|--------:|--------------|------:|---------:|---|
| Sacred Sun SP12-100 | 12 В      | 100 А·ч | 30 мин       | 28 кг | 12 900 ₽ | [Заказать] |

### 3.4. Кнопка «Заказать» и popup‑форма

При клике по кнопке **«Заказать»** открывается всплывающее окно (popup) с формой заявки.

**Поля формы:**

- **ФИО** — `input text`, обязательное;
- **Телефон** — `input tel`, обязательное, с маской формата `+7 (999) 999-99-99`;
- **Скрытые поля:**
  - `MODEL` — название выбранной модели аккумулятора;
  - `LINK` — ссылка на страницу товара.

**Требования к работе формы:**

- Валидация на клиенте (проверка обязательных полей, корректности телефона).
- При успешной отправке:
  - данные сохраняются в отдельный инфоблок (например, «Заявки калькулятора»); и/или
  - отправляются письмом на указанный e‑mail; и/или
  - передаются в CRM (если интеграция настроена).
- После отправки:
  - окно закрывается либо очищается;
  - пользователю показывается сообщение «Спасибо, ваша заявка отправлена».

Отправка формы **без перезагрузки страницы** (AJAX).

---

## 4. Технические требования

- **Платформа:** 1С‑Битрикс (редакция «Бизнес», D7 API, PHP 8.1+);
- **Размещение:** отдельная страница, например `/calculator/`;
- **Хранение данных:**
  - Каталог аккумуляторов — существующий или новый инфоблок каталога;
  - Заявки калькулятора — отдельный инфоблок или стандартные веб‑формы/CRM‑формы.
- **Фронтенд:**
  - Bootstrap 5 (сеток и базовых компонентов достаточно);
  - JavaScript: чистый JS или Bitrix JS (по договорённости).
- **Дизайн:**
  - Минималистичный, с использованием корпоративных цветов ZeusElectro;
  - Полная адаптивность (мобильные, планшеты, десктоп).
- **Popup‑форма:** реализуется на JS (модальное окно), без перезагрузки страницы.
- **SEO:**
  - `title`: «Калькулятор подбора аккумулятора ZeusElectro»;
  - `h1`: «Калькулятор подбора аккумуляторов»;
  - meta‑описание по согласованию с заказчиком.
- **Локализация:** русский язык.
- **Кэширование:**
  - Форма и логика расчёта — без кэширования;
  - Список аккумуляторов может кэшироваться (с учётом фильтров) по мере необходимости.
- **Тестирование:**
  - Актуальные версии Chrome, Firefox, Edge, Safari.

---

## 5. Требования к интеграции с каталогом

1. Данные по аккумуляторам хранятся в инфоблоке каталога:
   - один элемент = одна модель аккумулятора;
   - свойства элемента соответствуют колонкам Excel.

2. Для выборки данных использовать:
   - предпочтительно ORM‑подход (`\Bitrix\Iblock\Elements\ElementTable`, high‑level API);  
   - допускается использование `CIBlockElement::GetList`, если согласовано.

3. Производительность:
   - настроить индексы по ключевым свойствам (активность, время разряда, ёмкость, тип и т.п.);
   - предусмотреть возможность пагинации результатов при большом количестве моделей.

4. Импорт/обновление:
   - реализовать возможность обновления данных из Excel/CSV стандартными средствами 1С‑Битрикс (импорт каталога) или отдельным скриптом;
   - при обновлении:

     - не создавать дубликаты моделей;
     - обновлять значения свойств и цен.

---

## 6. Визуальная концепция (словесный макет)

**Верхний блок:**

- Логотип и/или заголовок сайта (как на остальных страницах).
- Заголовок страницы: `Калькулятор подбора аккумуляторов`.
- Небольшое описание (1–2 предложения), зачем нужен калькулятор.
- Форма ввода параметров в 1–2 строки (на десктопе) и в столбик (на мобильных).

**Ниже:**

- Заголовок: `Результаты подбора`.
- Таблица с моделями аккумуляторов.
- При отсутствии подходящих моделей — сообщение:  
  «По заданным параметрам подходящих моделей не найдено. Попробуйте изменить параметры или свяжитесь с менеджером.»

**Popup‑окно:**

- Заголовок: `Оформить заявку`.
- Поля: ФИО, телефон.
- Текст с подтверждением обработки персональных данных (с ссылкой на политику).

---

## 7. Этапы реализации и трудозатраты

Исходя из 8‑часового рабочего дня, трудозатраты оцениваются следующим образом:

1. **Анализ Excel‑файла и перенос данных в инфоблок** — **4 часа**  
2. **Создание страницы `/calculator/` и формы расчёта** — **8 часов**  
3. **Реализация логики вычислений и фильтрации по свойствам инфоблока** — **8 часов**  
4. **Верстка таблицы результатов и popup‑формы** — **4 часа**  
5. **Тестирование и внедрение** — **4 часа**

**Итого по трудозатратам:**  
**28 рабочих часов.**

---

## 8. Стоимость работ

Расчёт стоимости ведётся по ставке **2500 ₽/час**.

- Общее количество часов: **28 ч**
- Ставка: **2500 ₽/час**

Расчёт:

```text
28 × 2500 ₽ = 70 000 ₽
```

**Итоговая ориентировочная стоимость работ:**  
👉 **70 000 ₽** (семьдесят тысяч рублей).

При изменении объёма работ (доработки, дополнительные сценарии, интеграции, сложный импорт) стоимость подлежит пересмотру и согласуется с заказчиком дополнительно.

