# Техническое задание на разработку калькулятора подбора ИБП и АКБ

**Проект:** Страница-калькулятора подбора ИБП и аккумуляторных батарей  
**Сайт:** <https://zeuselectro.com/>  
**Платформа:** 1С-Битрикс (редакция «Бизнес»)  
**Заказчик:** ZeusElectro  
**Исполнитель:** Андрей Шевченко (VERDEXBIT)

---

## 1. Цель проекта

Реализовать на сайте интерактивный калькулятор, который:

1. Принимает от пользователя параметры нагрузки.
2. Предлагает подходящие по мощности и типу модели ИБП.
3. Для выбранного ИБП и требуемого времени автономии подбирает варианты конфигураций аккумуляторов (по количеству АКБ и ёмкости).
4. Выводит 1–3 оптимальных конфигурации с возможностью отправить запрос на КП менеджеру.

Калькулятор должен заменить текущий Excel-файл, используемый менеджерами, и повторять/расширять заложенную там логику расчёта автономии.

---

## 2. Источники данных и модель данных

### 2.1. Каталог ИБП

Данные по ИБП хранятся в инфоблоке каталога (существующий или отдельный). Для работы калькулятора у каждого ИБП должны быть как минимум:

- Название ИБП (модель);
- Номинальная мощность, кВт (P_nom_ИБП);
- Пиковая мощность, кВт (P_peak_ИБП) — если нет, использовать P_nom_ИБП;
- Тип корпуса (ИБП-тип): одно из значений:
  - `Модульный`
  - `Tower`
  - `Rack`
  - `Rack/Tower`
- Ссылка на карточку ИБП (детальная страница);
- Список допустимых вариантов **количества АКБ в одной цепочке/линейке**, например:  
  `24; 28; 30; 32; 34; ...; 60`  
  (хранить как строку/список в свойстве ИБП, либо как отдельную структуру);
- Дополнительно: артикул, производитель, прочие свойства по стандарту каталога.

### 2.2. Каталог АКБ

Отдельный инфоблок (либо раздел общего каталога) с моделями аккумуляторов:

- Модель АКБ, например: `RUCELF XX А·ч`;
- Номинальная ёмкость, А·ч;
- Тип (серия/линейка);
- Напряжение (12 В и т.п.);
- Ссылка на карточку АКБ;
- При необходимости: масса, габариты, цена.

### 2.3. Таблица расчёта (Excel)

Excel-файл (предоставленный заказчиком) используется как:

- Источник формул расчёта времени автономии при разных:
  - значениях нагрузки (P),
  - количестве АКБ в линейке,
  - количестве линеек,
  - ёмкости АКБ,
  - типе АКБ.
- Ориентир логики подбора (минимальная ёмкость, минимальное/максимальное кол-во АКБ и т.п.).

**Требование:** разработчик должен воспроизвести логику расчёта автономии на стороне сервера/JS на основе формул/методики из Excel.

---

## 3. Пользовательский сценарий

### Шаг 1. Ввод параметров нагрузки

Пользователь вводит:

1. **Номинальная мощность нагрузки, кВт**  
   - Тип: `number`, обязательное поле.

2. **Пиковая мощность, кВт**  
   - Тип: `number`, не обязательное.  
   - Если не заполнено, считается, что **P_peak = P_nom**.

После ввода пользователь нажимает кнопку **«Далее»** / **«Подобрать ИБП»**.

Система считает:

- `P_calc` — расчётная мощность для подбора ИБП:  
  - Если пиковая мощность задана: `P_calc = P_peak`;  
  - Если пиковая не задана: `P_calc = P_nom`.

---

### Шаг 2. Выбор типа ИБП

На основании `P_calc` система анализирует каталог ИБП и определяет, какие типы ИБП доступны:

- «Модульный»
- «Tower»
- «Rack»
- «Rack/Tower»

Тип считается **доступным**, если в каталоге есть хотя бы один ИБП с:

- `P_peak_ИБП >= P_calc` (если нет P_peak_ИБП — используется P_nom_ИБП).

На экране отображаются типы ИБП в виде набора чекбоксов/кнопок:

- `Не важно`
- `Модульный`
- `Tower`
- `Rack`
- `Rack/Tower`

Пользователь:

- Либо выбирает «Не важно» (используются все типы),
- Либо отмечает один или несколько конкретных типов ИБП.

---

### Шаг 3. Подбор и выбор модели ИБП

Система подбирает ИБП, удовлетворяющие условиям:

1. По мощности:  
   `P_peak_ИБП >= P_calc`  
   (при отсутствии отдельного P_peak_ИБП, использовать P_nom_ИБП).

2. По типу корпуса:
   - если выбран «Не важно» — не фильтровать по типу;
   - если выбран конкретный тип/типы — фильтровать по ним.

Результат выводится в виде **наборов карточек ИБП**, каждая карточка содержит:

- Модель ИБП;
- Тип корпуса (Tower / Rack / Rack/Tower / Модульный);
- Номинальная и/или пиковая мощность;
- Краткое описание (по желанию заказчика);
- Ссылка «Подробнее» на карточку ИБП (открывается в новой вкладке);
- Кнопка выбора: **«Выбрать этот ИБП»**.

Пользователь выбирает **один** ИБП (одна активная карточка).

---

### Шаг 4. Выбор требуемого времени автономии

После выбора ИБП калькулятор показывает выбор:

**Требуемое время автономии:**  

Варианты (радио-кнопки / селект):

- 5 минут
- 10 минут
- 15 минут
- 30 минут
- 45 минут
- 60 минут

Пользователь выбирает один вариант, например `30 минут`.

---

### Шаг 5. Расчёт конфигураций АКБ

Для выбранных:

- Нагрузки (`P_nom`, `P_peak`, `P_calc`);
- Выбранного ИБП (его мощность, тип, допустимые варианты количества АКБ в цепочке);
- Требуемого времени автономии (`T_req`, мин),

калькулятор подбирает варианты конфигураций АКБ.

#### 5.1. Входные данные для расчёта

- `P_calc` — расчётная мощность нагрузки, кВт.
- `T_req` — требуемое время автономии, мин.
- `UPS` — выбранная модель ИБП:
  - тип корпуса;
  - допустимые варианты количества АКБ в одной цепочке (например: 24, 28, 30 … 60);
- Множество моделей АКБ с различной ёмкостью.

#### 5.2. Логика подбора (общие принципы)

Калькулятор должен:

1. Использовать формулы и методику расчёта автономии из Excel-файла.
2. Перебирать возможные комбинации:
   - тип АКБ (серия/ёмкость, например `RUCELF 100 А·ч`, `RUCELF 120 А·ч` и т.п.);
   - число АКБ в цепочке (в пределах допустимых значений для данного ИБП);
   - число линеек (цепочек), от 1 до 4.

3. Для каждой комбинации рассчитать:
   - ориентировочное время автономии `T_calc` при 100% нагрузке (`P_calc`);
   - и проверить условие: `T_calc >= T_req`.

4. На основе найденных допустимых комбинаций сформировать **одну или несколько (до 3) оптимальных конфигураций**.

---

### 5.3. Требования к алгоритму поиска вариантов

Алгоритм должен реализовать следующие приоритеты:

1. **Минимальная ёмкость АКБ** с:
   - минимальным количеством линеек (от 1 до 4),
   - минимально допустимым количеством АКБ в линейке для данного ИБП,  
   при котором достигается требуемая автономия.

2. **Минимальная ёмкость АКБ** с:
   - минимальным количеством линеек,
   - **максимально** допустимым количеством АКБ в линейке,  
   при котором достигается требуемая автономия.

3. Если для одной линейки (1 цепочка) обеспечить требуемую автономию нельзя:
   - калькулятор увеличивает количество линеек до 2,
   - при необходимости — до 3 или 4 (максимум 4 линеек),
   - и повторяет расчёты до нахождения удовлетворяющих конфигураций.

Результат работы алгоритма:

- 1–3 **оптимальные конфигурации**, отсортированные по **общему количеству АКБ** (по возрастанию).

---

## 6. Вывод результата пользователю

Система выводит 1–3 найденных варианта конфигурации. Для каждого варианта отображается:

- **Модель ИБП** и **тип** (Tower/Rack/Модульный/Rack-Tower);
- **Мощность ИБП** (номинальная, при необходимости — пиковая);
- **Тип АКБ**, например: `RUCELF XX А·ч`;
- **Конфигурация по АКБ:**
  - Количество АКБ в одной цепочке / линейке;
  - Количество линеек (1–4);
  - Итоговое **общее количество АКБ**;
- **Ориентировочное время автономии** при 100% нагрузке:
  - Расчётное значение `T_calc` (можно округлять до минут);
- Ссылки:
  - ссылка на карточку ИБП;
  - ссылка на карточку АКБ (тип/модель);
- Кнопка: **«Получить КП»**.

---

## 7. Форма «Получить КП»

При нажатии на «Получить КП» для конкретной конфигурации открывается popup с формой:

**Поля:**

- ФИО — обязательное;
- Телефон — обязательное, с маской `+7 (999) 999-99-99`;
- E-mail — опционально (по согласованию);
- Скрытые поля:
  - выбранный ИБП (ID/название);
  - тип корпуса;
  - нагрузка (P_nom, P_peak, P_calc);
  - выбранное `T_req`;
  - выбранный тип АКБ;
  - количество АКБ в цепочке;
  - количество линеек;
  - общее количество АКБ;
  - расчётное время автономии `T_calc`;
  - ссылки на карточки ИБП и АКБ.

**Требования:**

- Отправка без перезагрузки страницы (AJAX).
- Сохранение заявки:
  - в отдельный инфоблок «Заявки КП калькулятора»; и/или
  - отправка письма менеджеру; и/или
  - интеграция с CRM (по отдельному ТЗ).
- Сообщение пользователю:  
  «Спасибо! Ваша заявка отправлена. Менеджер свяжется с вами в ближайшее время.»

---

## 8. Техническая реализация

- **Платформа:** 1С-Битрикс (редакция «Бизнес», D7 API, PHP 8.1+).
- **Инфоблоки:**
  - Каталог ИБП с необходимыми свойствами.
  - Каталог АКБ.
  - Инфоблок для заявок КП.
- **Фронтенд:**
  - Bootstrap 5;
  - JS (vanilla / Bitrix JS), AJAX-взаимодействие.
- **Бэкенд:**
  - Сервис/класс, реализующий:
    - расчёт `P_calc`;
    - поиск подходящих ИБП;
    - расчёт конфигураций АКБ с использованием формул из Excel;
    - формирование 1–3 оптимальных вариантов.
- **SEO и UX:**
  - Страница `/calculator/` с корректными title/meta;
  - адаптивная верстка.

---

## 9. Этапы реализации и трудозатраты (оценка)

1. Анализ Excel-файла и уточнение/перенос формул, проектирование модели данных — **4 ч**  
2. Подготовка инфоблоков/свойств (ИБП, АКБ, заявки) + базовый импорт данных — **6 ч**  
3. Бэкенд-сервис подбора:
   - логика подбора ИБП по мощности и типу;
   - логика подбора 1–3 конфигураций АКБ (1–4 линеек, min/max АКБ в цепочке);
   - API для фронтенда (AJAX/JSON)  
   — **14 ч**  
4. Фронтенд (форма шагов: нагрузка → тип ИБП → выбор ИБП → выбор автономии → вывод конфигураций) — **10 ч**  
5. Реализация формы «Получить КП» + обработчик (инфоблок/почта) — **4 ч**  
6. Тестирование, отладка, мелкие правки по ТЗ — **4 ч**

**Итого ориентировочно:**  
**4 + 6 + 14 + 10 + 4 + 4 = 42 часа.**

---

## 10. Стоимость работ

Расчёт ведётся по ставке **2500 ₽/час**.

- Общее количество часов: **42 ч**
- Ставка: **2500 ₽/час**

Пошаговый расчёт:

- 40 × 2500 ₽ = 100 000 ₽  
- 2 × 2500 ₽ = 5 000 ₽  

Суммарно:

```text
42 × 2500 ₽ = 105 000 ₽
```

**Итоговая ориентировочная стоимость полной реализации:**  
👉 **105 000 ₽** (сто пять тысяч рублей).

---

## 11. Предложение по MVP

Чтобы снизить стартовый бюджет и быстрее получить рабочий инструмент, можно разделить проект на **MVP** и **расширенную версию**.

### 11.1. MVP-версия (упрощённый подбор конфигурации АКБ)

**Что входит:**

- Полный путь пользователя:
  - ввод мощности нагрузки (номинальная/пиковая);
  - выбор типа ИБП;
  - выбор конкретного ИБП;
  - выбор времени автономии;
- Полноценный подбор ИБП по мощности и типу;
- Подбор **одной** конфигурации АКБ:
  - алгоритм находит первую подходящую комбинацию (тип АКБ, кол-во АКБ в цепочке, кол-во линеек до 4), обеспечивающую `T_calc >= T_req`;
  - без отдельного формирования «минимум/максимум по количеству АКБ в линейке» и без вывода 1–3 вариантов;
- Вывод **одной** конфигурации + кнопка «Получить КП»;
- Форма КП и сохранение заявки.

**Оценка по времени (MVP):**

1. Анализ Excel и модель данных — 4 ч  
2. Инфоблоки/свойства + импорт — 4 ч  
3. Бэкенд (упрощённый подбор одной конфигурации) — 10 ч  
4. Фронтенд (форма, карточки ИБП, выбор времени, вывод одной конфигурации) — 8 ч  
5. Форма «Получить КП» — 2 ч  
6. Тестирование — 2 ч  

**Итого по MVP:**  
4 + 4 + 10 + 8 + 2 + 2 = **30 ч**

Стоимость при ставке 2500 ₽/ч:

- 30 × 2500 ₽ =  
  - 3 × 25 000 ₽ = 75 000 ₽

👉 **MVP: ~75 000 ₽**

---

### 11.2. Расширение до полной версии (после MVP)

После запуска MVP можно добавить:

- Логику вычисления **1–3 оптимальных конфигураций**:
  - вариант с минимальной ёмкостью при минимальном количестве АКБ в цепочке;
  - вариант при максимальном количестве АКБ в цепочке;
  - дополнительный вариант, минимизирующий общее число АКБ;
- Более продвинутую сортировку и отображение вариантов;
- Дополнительные подсказки/описания для пользователя.

Дополнительное время по сравнению с MVP: примерно **12 ч**, что даёт:

- 12 × 2500 ₽ =  
  - 1 × 25 000 ₽ = 25 000 ₽  
  - плюс ещё 5 000 ₽ (половина 25 000 ₽)  
  = **30 000 ₽**

Итого:

- **Полная версия сверху MVP:** 30 ч + 12 ч = те самые ~42 ч / 105 000 ₽.
